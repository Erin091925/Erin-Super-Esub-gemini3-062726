Super, please improve previous design by keeping all original features and adding additional features that 1. please make gemini-3.1-flash-lite default model for llm features (user can select other models and modify prompt). 2. please adding wow visualization effects for llm execution, interactive indicators, live log and wow dashboard with 6 wow interactive graph Perform Optical Character Recognition (OCR) on the provided document pages. The document contains both Traditional Chinese and English text. Allow the user to select the specific pages to process. After OCR, present the results for user review and modification. The OCR engine should support multiple language models (e.g., traditional Chinese and English) and allow selection between Python-based OCR packages (mentioning specific packages like EasyOCR, Tesseract with appropriate language packs) or LLM-based OCR (defaulting to Gemini-3.1-Flash-Lite, with options to select other models and modify prompts).

User has provided or selected a 'skill.md' file. Allow the user to modify this file. Then, use an AI model (defaulting to Gemini-3.1-Flash-Lite, with options to select other models) and a user-defined prompt to improve the content of the 'skill.md' file. Present the updated 'skill.md' for user review and modification, and provide options to download the updated file.

Analyze the provided medical device submission materials (MD, TXT, JSON, PDF). Extract and categorize key sections such as administrative data, non-clinical test results, software validation, material safety, sterilization, and shelf-life data. Identify and summarize any potential compliance issues or discrepancies based on standard regulatory requirements. Output the analysis in a structured markdown format with critical keywords highlighted. As an AI assistant, develop a feature for the medical device submission agent that performs advanced analysis of uploaded documents (PDF, MD, TXT, JSON). This feature should automatically identify and categorize key sections such as 'Introduction', 'Device Description', 'Intended Use', 'Risk Analysis', 'Clinical Evaluation', and 'Sterilization'. For each identified section, extract critical data points relevant to a regulatory submission (e.g., device specifications, material composition, sterilization validation results, clinical trial endpoints, adverse event data). Generate a structured summary in markdown format, highlighting these extracted data points. Additionally, implement a mechanism to identify and extract potential keywords from the document and present them in a distinct style (e.g., bolded and colored coral) within the summary. The agent should also provide a confidence score for the accuracy of its analysis and summarization. If a PDF is provided, allow the user to specify page ranges for initial processing before OCR if that is selected.

As an AI assistant, build an interactive Q&A module for the medical device submission agent. This module should leverage the processed submission materials and a 'skill.md' file (either user-provided or a default one). The agent should be able to generate a comprehensive set of initial review questions based on the submission content and the 'skill.md'. Subsequently, the user should be able to interactively refine the answers to these questions. The agent should then use these refined answers and the original submission materials to generate a detailed review report. Ensure the system allows for dynamic model selection (e.g., Gemini models, or others if specified) and prompt customization for both question generation and answer refinement. The 'skill.md' file should be editable and downloadable, with an option to prompt the agent to improve its content.

As an AI assistant, integrate a version control and audit trail system into the medical device submission agent. This system should automatically track all versions of uploaded medical device submission materials (PDF, MD, TXT, JSON), OCR results, cleaned and organized submission summaries, 'skill.md' files, generated questions and answers, and final review reports. For each tracked item, record the timestamp, the user action (upload, modification, download, generation), and the specific version created. Provide users with an interface to view the history of changes, compare different versions of a document or report, and revert to a previous version if necessary. This feature should also generate a downloadable audit log in a structured format (e.g., CSV or JSON) suitable for regulatory compliance. Develop an intelligent keyword extraction and tagging system. After OCR processing, the system should identify key terms and phrases within the medical device submission documents. These keywords should be color-coded (e.g., coral) within the generated markdown summary and also stored for potential advanced search functionalities.

Implement an AI-powered document summarization feature. The system should take the processed OCR text from the medical device submission materials and generate a concise summary highlighting the key aspects of the submission. The summary should be presented in markdown format.

Integrate a version control system for all generated and modified documents, including the original uploaded materials, OCR results, submission summaries, skill.md, and review reports. Users should be able to view version history, revert to previous versions, and compare changes between versions.

Develop an intelligent keyword extraction and tagging system. After OCR processing, the system should identify key terms and phrases within the medical device submission documents. These keywords should be color-coded (e.g., coral) within the generated markdown summary and also stored for potential advanced search functionalities. Please also adding 3 additional wow ai features this app. Please fix blank screen bugs and iterate until get excellent results. Ending with 20 compehensive follow up questions.
